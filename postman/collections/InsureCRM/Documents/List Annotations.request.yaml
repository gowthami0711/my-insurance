$kind: http-request
name: List Annotations
method: GET
url: '{{baseUrl}}/api/documents/{{documentId}}/annotations'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
