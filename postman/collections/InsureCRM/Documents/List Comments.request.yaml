$kind: http-request
name: List Comments
method: GET
url: '{{baseUrl}}/api/documents/{{documentId}}/comments'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
