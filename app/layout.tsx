import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import { Providers } from "./providers";
import "./globals.css";

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "InsureCRM | Claims Management Dashboard",
  description: "Role-based insurance claims management platform for processing and adjudicating claims across multiple channels.",
  keywords: ["insurance", "claims", "CRM", "dashboard", "risk assessment"],
  authors: [{ name: "InsureCRM" }],
  robots: "noindex, nofollow", 
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${poppins.variable} ${poppins.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}